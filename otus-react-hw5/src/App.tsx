import React from 'react';
import ReactDOM from 'react-dom';
import ApiFetcher from '../src/components/api-fetcher/api-fetcher';

const App: React.FC = () => {
  return (
      <div>
        <ApiFetcher />
      </div>
  );
};

export default App;